@using Sandbox;
@using Sandbox.UI;
@inherits Panel
@namespace VoxelParty.UI

<root onclick=@(()=>{this.ParentOfType<OptionPanel>().Source.OnOptionClick(this); this.ParentOfType<OptionPanel>().Source.Close();})>
    @Text
</root>